# Mac local config: nginx_h1_tls.conf
# Runs as current user, no root required.
worker_processes 1;
error_log /tmp/eec_nginx_error.log warn;
pid /tmp/eec_nginx.pid;

events { worker_connections 512; }

http {
    include /opt/homebrew/etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;
    access_log off;

    server {
        listen 8443 ssl;
        http2 off;
        ssl_certificate     /Users/hassaanali/coding-workspace/energy-eff-streaming/server/assets/../tls/server.crt;
        ssl_certificate_key /Users/hassaanali/coding-workspace/energy-eff-streaming/server/assets/../tls/server.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        root /Users/hassaanali/coding-workspace/energy-eff-streaming/server/assets;

        location /bulk/ { autoindex off; }
        location /hls/ {
            add_header Cache-Control no-cache;
            types { application/vnd.apple.mpegurl m3u8; video/mp2t ts; }
        }
        location /hls.key { alias /Users/hassaanali/coding-workspace/energy-eff-streaming/server/assets/../tls/hls.key; }
    }
}
